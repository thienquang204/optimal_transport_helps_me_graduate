# Direct runtime dependencies. Versions are pinned to the tested `thangbo`
# environment so local, CI, and Docker runs use the same APIs.
torch==2.13.0
torchvision==0.28.0
datasets==5.0.1
huggingface-hub==1.27.0
Pillow==12.3.0
tqdm==4.67.1

# CUDA FAISS. PyPI GPU wheels are Linux x86-64 only. The Dockerfile builds
# FAISS from source for Linux ARM64 (GX10/GB10); do not install faiss-cpu there
# because both distributions provide the same Python module.
faiss-gpu-cu12; platform_system == "Linux" and platform_machine == "x86_64"
faiss-cpu; platform_system != "Linux"
matplotlib
